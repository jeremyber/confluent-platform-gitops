# Confluent for Kubernetes - flink-demo-rbac Cluster Overlay
# Cluster-specific configuration overrides

# Additional pod annotations for flink-demo-rbac cluster identification
podAnnotations:
  cluster: flink-demo-rbac

# Watch additional namespaces for group-based Flink resources
namespaceList:
  - flink
  - flink-shapes
  - flink-colors
  - kafka
  - operator
