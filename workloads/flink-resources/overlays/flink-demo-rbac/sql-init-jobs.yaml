# Jobs to initialize Flink SQL Catalogs, Databases, and ComputePools via CMF API
# These jobs run once to create the resources in CMF
---
# Shapes SQL Initialization Job
apiVersion: batch/v1
kind: Job
metadata:
  name: shapes-sql-init
  namespace: flink-shapes
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: sql-init
          image: curlimages/curl:8.1.2
          env:
            - name: OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: cmf-mds-oauth-client
                  key: clientId
            - name: OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: cmf-mds-oauth-client
                  key: clientSecret
          command:
            - /bin/sh
            - -c
            - |
              set -e

              CMF_URL="http://cmf-service.operator.svc.cluster.local:80/cmf/api/v1"
              KEYCLOAK_URL="http://keycloak.keycloak.svc.cluster.local:8080/realms/confluent/protocol/openid-connect/token"

              echo "Obtaining OAuth token..."
              TOKEN_RESPONSE=$(curl -s -X POST ${KEYCLOAK_URL} \
                -H "Content-Type: application/x-www-form-urlencoded" \
                -d "grant_type=client_credentials" \
                -d "client_id=${OAUTH_CLIENT_ID}" \
                -d "client_secret=${OAUTH_CLIENT_SECRET}")

              ACCESS_TOKEN=$(echo ${TOKEN_RESPONSE} | grep -o '"access_token":"[^"]*' | cut -d'"' -f4)

              if [ -z "${ACCESS_TOKEN}" ]; then
                echo "Failed to obtain access token"
                echo "Response: ${TOKEN_RESPONSE}"
                exit 1
              fi

              echo "Token obtained successfully"

              echo "[1/5] Deleting old CMF Secret kafka-oauth-secret-shapes if it exists..."
              curl -s -o /dev/null -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X DELETE ${CMF_URL}/secrets/kafka-oauth-secret-shapes

              echo "[1/5] Creating CMF Secret: kafka-oauth-secret-shapes"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/secrets \
                -d @/config/secret/secret.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[2/7] Creating secret mapping: kafka-conn-secret-id-shapes -> kafka-oauth-secret-shapes"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/environments/shapes-env/secret-mappings \
                -d @/config/secret-mapping/secret-mapping.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "Checking if sr-oauth-secret-shapes exists..."
              SECRET_EXISTS=$(curl -s -o /dev/null -w "%{http_code}" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X GET ${CMF_URL}/secrets/sr-oauth-secret-shapes)

              if [ "${SECRET_EXISTS}" = "404" ]; then
                echo "[3/7] Creating CMF Secret: sr-oauth-secret-shapes"
                RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                  -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                  -X POST ${CMF_URL}/secrets \
                  -d @/config/sr-secret/secret.json)
                HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
                RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
                echo "  -> HTTP $HTTP_CODE"
                if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                  echo "  -> Error: $RESPONSE_BODY"
                fi
              else
                echo "[3/7] Secret sr-oauth-secret-shapes already exists (HTTP ${SECRET_EXISTS})"
              fi

              echo "[4/7] Creating secret mapping: sr-oauth-secret-id-shapes -> sr-oauth-secret-shapes"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/environments/shapes-env/secret-mappings \
                -d @/config/sr-secret-mapping/secret-mapping.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[5/7] Creating KafkaCatalog: shapes-catalog"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/catalogs/kafka \
                -d @/config/catalog/catalog.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[6/7] Creating KafkaDatabase: shapes-database in shapes-catalog"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/catalogs/kafka/shapes-catalog/databases \
                -d @/config/database/database.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[7/7] Creating ComputePool: shapes-pool in shapes-env"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/environments/shapes-env/compute-pools \
                -d @/config/pool/compute-pool.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo ""
              echo "Shapes environment initialization complete"
          volumeMounts:
            - name: catalog-config
              mountPath: /config/catalog
            - name: database-config
              mountPath: /config/database
            - name: pool-config
              mountPath: /config/pool
            - name: secret-config
              mountPath: /config/secret
            - name: secret-mapping-config
              mountPath: /config/secret-mapping
            - name: sr-secret-config
              mountPath: /config/sr-secret
            - name: sr-secret-mapping-config
              mountPath: /config/sr-secret-mapping
      volumes:
        - name: catalog-config
          configMap:
            name: shapes-catalog-config
        - name: database-config
          configMap:
            name: shapes-database-config
        - name: pool-config
          configMap:
            name: shapes-pool-config
        - name: secret-config
          configMap:
            name: kafka-oauth-secret-config
        - name: secret-mapping-config
          configMap:
            name: shapes-secret-mapping-config
        - name: sr-secret-config
          configMap:
            name: sr-oauth-secret-config
        - name: sr-secret-mapping-config
          configMap:
            name: shapes-sr-secret-mapping-config
---
# Colors SQL Initialization Job
apiVersion: batch/v1
kind: Job
metadata:
  name: colors-sql-init
  namespace: flink-colors
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: sql-init
          image: curlimages/curl:8.1.2
          env:
            - name: OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: cmf-mds-oauth-client
                  key: clientId
            - name: OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: cmf-mds-oauth-client
                  key: clientSecret
          command:
            - /bin/sh
            - -c
            - |
              set -e

              CMF_URL="http://cmf-service.operator.svc.cluster.local:80/cmf/api/v1"
              KEYCLOAK_URL="http://keycloak.keycloak.svc.cluster.local:8080/realms/confluent/protocol/openid-connect/token"

              echo "Obtaining OAuth token..."
              TOKEN_RESPONSE=$(curl -s -X POST ${KEYCLOAK_URL} \
                -H "Content-Type: application/x-www-form-urlencoded" \
                -d "grant_type=client_credentials" \
                -d "client_id=${OAUTH_CLIENT_ID}" \
                -d "client_secret=${OAUTH_CLIENT_SECRET}")

              ACCESS_TOKEN=$(echo ${TOKEN_RESPONSE} | grep -o '"access_token":"[^"]*' | cut -d'"' -f4)

              if [ -z "${ACCESS_TOKEN}" ]; then
                echo "Failed to obtain access token"
                echo "Response: ${TOKEN_RESPONSE}"
                exit 1
              fi

              echo "Token obtained successfully"

              echo "[1/5] Deleting old CMF Secret kafka-oauth-secret-colors if it exists..."
              curl -s -o /dev/null -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X DELETE ${CMF_URL}/secrets/kafka-oauth-secret-colors

              echo "[1/5] Creating CMF Secret: kafka-oauth-secret-colors"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/secrets \
                -d @/config/secret/secret.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[2/7] Creating secret mapping: kafka-conn-secret-id-colors -> kafka-oauth-secret-colors"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/environments/colors-env/secret-mappings \
                -d @/config/secret-mapping/secret-mapping.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "Checking if sr-oauth-secret-colors exists..."
              SECRET_EXISTS=$(curl -s -o /dev/null -w "%{http_code}" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X GET ${CMF_URL}/secrets/sr-oauth-secret-colors)

              if [ "${SECRET_EXISTS}" = "404" ]; then
                echo "[3/7] Creating CMF Secret: sr-oauth-secret-colors"
                RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                  -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                  -X POST ${CMF_URL}/secrets \
                  -d @/config/sr-secret/secret.json)
                HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
                RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
                echo "  -> HTTP $HTTP_CODE"
                if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                  echo "  -> Error: $RESPONSE_BODY"
                fi
              else
                echo "[3/7] Secret sr-oauth-secret-colors already exists (HTTP ${SECRET_EXISTS})"
              fi

              echo "[4/7] Creating secret mapping: sr-oauth-secret-id-colors -> sr-oauth-secret-colors"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/environments/colors-env/secret-mappings \
                -d @/config/sr-secret-mapping/secret-mapping.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[5/7] Creating KafkaCatalog: colors-catalog"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/catalogs/kafka \
                -d @/config/catalog/catalog.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[6/7] Creating KafkaDatabase: colors-database in colors-catalog"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/catalogs/kafka/colors-catalog/databases \
                -d @/config/database/database.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo "[7/7] Creating ComputePool: colors-pool in colors-env"
              RESPONSE=$(curl --silent -w "\n%{http_code}" -H "Content-Type: application/json" \
                -H "Authorization: Bearer ${ACCESS_TOKEN}" \
                -X POST ${CMF_URL}/environments/colors-env/compute-pools \
                -d @/config/pool/compute-pool.json)
              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
              echo "  -> HTTP $HTTP_CODE"
              if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
                echo "  -> Error: $RESPONSE_BODY"
              fi

              echo ""
              echo "Colors environment initialization complete"
          volumeMounts:
            - name: catalog-config
              mountPath: /config/catalog
            - name: database-config
              mountPath: /config/database
            - name: pool-config
              mountPath: /config/pool
            - name: secret-config
              mountPath: /config/secret
            - name: secret-mapping-config
              mountPath: /config/secret-mapping
            - name: sr-secret-config
              mountPath: /config/sr-secret
            - name: sr-secret-mapping-config
              mountPath: /config/sr-secret-mapping
      volumes:
        - name: catalog-config
          configMap:
            name: colors-catalog-config
        - name: database-config
          configMap:
            name: colors-database-config
        - name: pool-config
          configMap:
            name: colors-pool-config
        - name: secret-config
          configMap:
            name: kafka-oauth-secret-config
        - name: secret-mapping-config
          configMap:
            name: colors-secret-mapping-config
        - name: sr-secret-config
          configMap:
            name: sr-oauth-secret-config
        - name: sr-secret-mapping-config
          configMap:
            name: colors-sr-secret-mapping-config
